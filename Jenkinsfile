pipeline {
  
  agent any
  
  stages {
    
    stage("build") {
      steps {
	      echo 'building the app'
      }
    }

    stage("test") {
      steps {
        echo 'test complete'
      }
    }

    stage("deploy") {
      steps {
        echo 'deploying'
      }
    }
  }
}